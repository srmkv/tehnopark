import { SyntheticEvent } from 'react';
type Props = {
    onClick: (e: SyntheticEvent<HTMLElement, Event>) => void;
};
declare const FetchError: ({ onClick }: Props) => JSX.Element;
export default FetchError;
