/// <reference types="react" />
import { IUser } from '@giphy/js-types';
type Props = {
    user: IUser;
    className?: string;
};
declare const Avatar: ({ user, className }: Props) => JSX.Element;
export default Avatar;
