/// <reference types="react" />
type Props = {
    className?: string;
    placeholder?: string;
    clear?: boolean;
    autoFocus?: boolean;
    searchDebounce?: number;
    initialTerm?: string;
    onEnter?: (term: string) => void;
};
declare const SearchBar: {
    ({ className, placeholder, clear, autoFocus, searchDebounce, onEnter, }: Props): JSX.Element;
    className: string;
};
export default SearchBar;
