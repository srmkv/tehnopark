export type SearchTheme = {
    mode: 'dark' | 'light';
    searchbarHeight: number;
    smallSearchbarHeight: number;
    condensedMediaQuery?: string;
    condensedMode?: boolean;
    hideCancelButton?: boolean;
};
export declare const initTheme: (theme?: Partial<SearchTheme>) => SearchTheme;
export declare const getSize: (theme: SearchTheme, includeWidth?: boolean) => import("@emotion/react").SerializedStyles;
