"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getViewManagerConfig = getViewManagerConfig;
exports.runViewManagerCommand = runViewManagerCommand;

var _reactNative = require("react-native");

function getViewManagerConfig(moduleName) {
  if (_reactNative.UIManager.getViewManagerConfig) {
    // RN >= 0.58
    return _reactNative.UIManager.getViewManagerConfig(moduleName);
  } // RN < 0.58


  return _reactNative.UIManager[moduleName];
}

function runViewManagerCommand(options) {
  const {
    args = [],
    command,
    moduleName,
    nodeHandle
  } = options;

  if (!nodeHandle) {
    return null;
  }

  const viewManagerConfig = getViewManagerConfig(moduleName);
  return _reactNative.UIManager.dispatchViewManagerCommand(nodeHandle, viewManagerConfig.Commands[command], args);
}
//# sourceMappingURL=viewManager.js.map