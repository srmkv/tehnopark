import { UIManager } from 'react-native';
export function getViewManagerConfig(moduleName) {
  if (UIManager.getViewManagerConfig) {
    // RN >= 0.58
    return UIManager.getViewManagerConfig(moduleName);
  } // RN < 0.58


  return UIManager[moduleName];
}
export function runViewManagerCommand(options) {
  const {
    args = [],
    command,
    moduleName,
    nodeHandle
  } = options;

  if (!nodeHandle) {
    return null;
  }

  const viewManagerConfig = getViewManagerConfig(moduleName);
  return UIManager.dispatchViewManagerCommand(nodeHandle, viewManagerConfig.Commands[command], args);
}
//# sourceMappingURL=viewManager.js.map